{
    "name": "@formex/api",
    "private":"true",
    "scripts": {
        "dev":"composer run dev",
        "test":"composer run test",
        "openapi:export": "composer run openapi:export",
        "clean":"rm -rf node_modules vendor .cache",
        "composer:install": "composer install"
    }
}
